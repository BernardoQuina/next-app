mutation NewPost ($title: String!, $body: String!, $published: Boolean!) {
  createPost(
    title: $title
    body: $body
    published: $published
  ) {
    id
    title
    body
    published
  }
}
