query SinglePost ($postId: String) {
    post(where: { id: $postId }) {
      id
      title
      body
      images
      published
      createdAt
      updatedAt
      author {
        id
        name
        photo
      }
      comments {
        ...Comment
      }
    }
  }