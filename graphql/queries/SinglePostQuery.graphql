query SinglePostQuery ($postId: String!) {
    post(where: { id: $postId }) {
      id
      title
      body
      published
      createdAt
      updatedAt
      author {
        name
        id
      }
      comments {
        id
        text
        author {
          id
          name
        }
      }
    }
  }