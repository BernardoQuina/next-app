query SinglePost ($postId: String) {
    post(where: { id: $postId }) {
      id
      title
      body
      images
      published
      createdAt
      updatedAt
      author {
        name
        id
      }
      comments {
        ...Comment
      }
    }
  }