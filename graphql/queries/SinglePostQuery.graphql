query SinglePost ($postId: String!) {
    post(where: { id: $postId }) {
      id
      title
      body
      published
      createdAt
      updatedAt
      author {
        name
        id
      }
      comments {
        id
        createdAt
        text
        author {
          id
          name
        }
      }
    }
  }